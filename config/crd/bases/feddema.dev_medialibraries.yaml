---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: medialibraries.feddema.dev
spec:
  group: feddema.dev
  names:
    kind: MediaLibrary
    listKind: MediaLibraryList
    plural: medialibraries
    singular: medialibrary
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The name of the PersistentVolumeClaim
      jsonPath: .status.actualPVCName
      name: PVC
      type: string
    - description: The size of the MediaLibrary
      jsonPath: .status.resources.requests.storage
      name: Size
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MediaLibrary is the Schema for the medialibraries API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MediaLibrarySpec defines the desired state of MediaLibrary
            properties:
              crossNamespaceAccess:
                default: true
                description: CrossNamespaceAccess defines whether the MediaLibrary
                  should be accessible from other namespaces.
                type: boolean
              permissions:
                description: Permissions defines POSIX-like permissions to set on
                  the MediaLibrary.
                properties:
                  fsGroup:
                    default: 5000
                    description: FSGroup is the group ID to set on the MediaLibrary
                      files.
                    format: int64
                    type: integer
                  runAsGroup:
                    default: 5000
                    description: RunAsGroup is the group ID to run the MediaLibrary
                      as.
                    format: int64
                    type: integer
                  runAsUser:
                    default: 5000
                    description: RunAsUser is the user ID to run the MediaLibrary
                      as.
                    format: int64
                    type: integer
                  supplementalGroups:
                    description: SupplementalGroups is the supplemental group IDs
                      to set on the MediaLibrary files.
                    items:
                      format: int64
                      type: integer
                    type: array
                type: object
              pvc:
                description: PVC defines the PersistentVolumeClaim to use for the
                  MediaLibrary.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations to add to the PersistentVolumeClaim.
                    type: object
                  pvcName:
                    description: PVCName is name of a pre-existing PersistentVolumeClaim
                      to use for the MediaLibrary.
                    type: string
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    description: Size defines the size of the PersistentVolumeClaim
                      to create for the MediaLibrary.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storageClassName:
                    description: StorageClassName is the name of the StorageClass
                      to use for the PersistentVolumeClaim.
                    type: string
                    x-kubernetes-validations:
                    - message: Value is immutable
                      rule: self == oldSelf
                type: object
            required:
            - pvc
            type: object
          status:
            description: MediaLibraryStatus defines the observed state of MediaLibrary
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              effectivePVC:
                description: EffectivePVC is the name of the PersistentVolumeClaim
                  used for this MediaLibrary.
                type: string
              initialized:
                default: false
                description: Initialized indicates whether the MediaLibrary has been
                  initialized.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
